# Merge these values into the matching sections of Geyser's config.yml.
# Do not expose this Bedrock listener to players.
bedrock:
  address: 10.10.0.30
  port: 19134

java:
  auth-type: floodgate

advanced:
  bedrock:
    validate-bedrock-login: false
    use-waterdogpe-forwarding: false
