# OniLink 0.1.3

OniLink 0.1.3 makes secure multi-backend setup a first-class control-plane workflow. An administrator can now add a native BDS + Endstone backend, generate its isolated key, and receive both matched configurations without searching inside the raw editor.

## First-class backend onboarding

- **Add Backend** has its own main-navigation entry and is linked directly from **Backends** and **Configuration**.
- A three-stage setup view explains which BDS endpoint and trusted OniLink source CIDR to enter.
- The wizard still validates the complete proxy configuration, rejects stale revisions and duplicate routes, writes a rollback backup, and preserves hub/join/failover policy.
- OniLink generates one random 32-byte key per backend and installs its proxy-side copy at `secrets/<backend>.key` with owner-only permissions.
- The result shows the exact non-secret proxy properties already saved, the one-time key, and a complete production-locked `onibridge.toml`.
- Exact Pterodactyl upload paths, backend-first restart order, hook confirmation, and `/server <backend>` test command are shown beside the downloads.
- The generation button is locked while the request is in progress to prevent accidental duplicate submissions.

No forwarding secret is written into `config.properties`, returned in the proxy-properties snippet, or placed in the dashboard audit record. The Endstone configuration uses `active_secret_file`; operators do not need to create a new startup variable for each generated backend.

## Upgrade

1. Back up `config.properties`, `dashboard/`, `allowlist.properties` if present, and forwarding key files.
2. Restart OniLink. The stable updater downloads and verifies the v0.1.3 JAR while preserving the active configuration and keys.
3. Sign in as an admin or owner and open **Add Backend**.

No egg reimport or native plugin replacement is required solely for this dashboard change. New installations should use the v0.1.3 egg and version-aligned artifacts.

## Verify downloads

```bash
gh release download v0.1.3 \
  --repo TheNINJALLO/OniLink \
  --dir onilink-release
cd onilink-release
sha256sum -c SHA256SUMS
```

## Checksums and provenance

The release bundle was built from commit `138f2b0a4e58f6521252806861c0c0ccb8fc94fd` by [Linux Release Artifacts run 32255775493](https://github.com/TheNINJALLO/OniLink/actions/runs/32255775493). [Cross-platform CI run 32255775525](https://github.com/TheNINJALLO/OniLink/actions/runs/32255775525) passed the OniLink, OniBridge-Geyser, Python packaging/egg/ABI, Ubuntu native, and Windows native jobs.

The downloaded workflow bundle was independently checked against every entry in `SHA256SUMS`. The complete release-file hashes are:

| File | SHA-256 |
| --- | --- |
| `OniLink.jar` | `f6c63e802e29868afe0070aad38fa31d102b418218938dec641d289ac2d2374e` |
| `OniBridge-Geyser.jar` | `8383e40e885dd192c1aedd31cdf975588875d799e5d4d6e730ab8111b3475c3c` |
| `onibridge-0.1.3-bds-1.26.44.3-linux-x86_64.so` | `70c79a9c2c9da7820539a1a11f9a08a40cad802eb04b8b1e776ff7efd175863b` |
| `onilink.properties.example` | `018172f72eb18013b40ffadb6e10e036d52007468c85dd52e77f9112f452375f` |
| `onibridge.toml.example` | `29014302bbdf0737b42713b4ab031f1f4baee3e33ccfb5edbec2f9177f6915ac` |
| `onibridge-geyser.properties.example` | `1385828b914a192e8113f1e592a56236f0874c45882abe9f0726b84bc247f037` |
| `egg-onilink.json` | `1817a2d38fa5f7c3732f16ed259951abeabe837b20f3cb959fb4b7171a2ea5d4` |
| `start-onilink.sh` | `5e1d442a08dae66ff3bdad28c1b8079897e48e48c6ddeb47cc8b972d833c15eb` |
| `onibridge-profile-1.26.44.3-linux-x86_64.json` | `e8a2ab4453d1f0a7515b914150b2a743e1077f908fe8d596da9ed330e0f4dd89` |
| `linux-compatibility-manifest.json` | `4de19ba3ab05f75e3ab2cb865c1b73a776be344b218fa16f8cc19928bf06796a` |
| `SHA256SUMS` | `00a184d95507823cd4d28b77737328154a2bcfe1653a6a1241d749d510fadb8f` |

Both JAR manifests report version `0.1.3`. The native artifact is production-ready for the exact reviewed Linux profile, requires only the approved runtime libraries, imports no glibc symbol newer than `GLIBC_2.35`, and passes the C++ runtime policy.

No Mojang/Microsoft BDS archive or executable is distributed.
