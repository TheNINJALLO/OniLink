# Binary fixtures

The SDK tests synthesize minimal ELF64 and PE32+ files at runtime. They contain no Mojang code and are deleted with each temporary test directory. Real BDS binaries, PDBs, dumps, and disassemblies are forbidden here.

