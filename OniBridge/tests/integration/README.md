# Native integration tests

Live tests require the user's own EULA-authorized BDS installations for both supported platforms. They must prove startup, normal login preservation, forwarding rejection paths, identity availability before storage, reconnect behavior, command behavior, crash-free shutdown, and coexistence with Endstone.

No fixture-only result may satisfy a release gate.
