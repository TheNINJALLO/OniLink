# OniBridge documentation

The maintained documentation is shared from the repository-level [`docs`](../../docs/) directory so the proxy, token protocol, native hook, acquisition tooling, and release gates remain consistent.
