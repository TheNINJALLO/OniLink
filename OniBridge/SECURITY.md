# OniBridge security

Do not report security vulnerabilities in a public issue. Follow the private reporting process in the repository-level [security policy](../SECURITY.md).

OniBridge fails closed when its BDS fingerprint, reviewed compatibility profile, generated adapter, trusted proxy CIDR, forwarding key material, or pre-storage hook cannot be validated. Fixture profiles and harness results are never production compatibility evidence.
